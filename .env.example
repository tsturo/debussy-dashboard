# DEBUSSY_PROJECT_PATH: Absolute path to the Debussy project root directory
# This directory must contain:
#   - .beads/ directory (beads workflow tracker)
#   - .claude/mailbox/ directory (agent communication)
# If not set, defaults to current working directory (process.cwd())
#
# Examples:
#   DEBUSSY_PROJECT_PATH=/Users/yourname/projects/my-debussy-project
#   DEBUSSY_PROJECT_PATH=/home/dev/workspace/debussy-app
#
# Validation requirements:
#   - Path must exist and be a readable directory
#   - Must contain .beads/ subdirectory
#   - Must contain .claude/mailbox/ subdirectory
DEBUSSY_PROJECT_PATH=/path/to/your/debussy/project

POLLING_INTERVAL=5000

DEBUG=false
